'use strict';

// Imports
const TransportHttp = require('./transport_http');

// Typedef
/**
 * @typedef {Object} TransportOptions
 * @property {Logger} [logger] -
 */

module.exports = {
    TransportHttp
};
